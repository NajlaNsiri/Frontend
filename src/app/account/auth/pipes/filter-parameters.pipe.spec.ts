import { FilterParametersPipe } from './filter-parameters.pipe';

describe('FilterParametersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParametersPipe();
    expect(pipe).toBeTruthy();
  });
});
